# library/styles

Confirmed Style definitions (door style, material/colour, finish, handle
combinations) — e.g. the "Navarra / Walnut / Chrome illuminated" style
seen in the existing order_form library.

Schema: `../../schemas/style.schema.yaml`
Spec: `../../docs/Specifications/StyleSpecification.md`

Style option text was not machine-extractable from the existing DXF/PDF
order form (no surviving labels) — entries here must be confirmed
directly against the real form, not assumed from the visual layout. See
`../../docs/KnowledgeCapture.md`.
