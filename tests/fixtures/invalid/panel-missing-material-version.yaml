x-dtml-schema: ../../../schemas/part.schema.yaml

# Deliberately invalid: material.object_version is missing, which
# reference.schema.yaml requires. Proves the validator actually enforces
# the reference contract rather than accepting bare strings.

namespace: cuttingedgebespoke.part
id: panel_missing_material_version
name: "Invalid fixture — missing material object_version"
schema_version: "0.1"
object_version: "1.0.0"
status: draft
provenance:
  source_type: narrated
  source_reference: "Schema foundation work package, 2026-07-30"
  captured_by: lucian
  captured_at: "2026-07-30T00:00:00Z"
  confidence: confirmed
  review_status: approved
  reviewed_by: lucian
  evidence: []

material:
  ref: cuttingedgebespoke.material.egger_w1000_18mm

thickness:
  literal:
    value: 18
    unit: mm

construction: single_18mm

dimensions:
  width:
    literal:
      value: 600
      unit: mm
  height:
    literal:
      value: 720
      unit: mm

reference_face: front

feature_instances:
  - feature:
      ref: cuttingedgebespoke.feature.shelf_pin_array
      object_version: "1.0.0"
    position:
      x:
        literal:
          value: 20
          unit: mm
      y:
        literal:
          value: 20
          unit: mm
    reference_face: front
