x-dtml-schema: ../../../schemas/features/hole_array.schema.yaml

# Specialised case not covered by examples/shelf_pin_array.feature.yaml
# (which is blind): proves hole_form: through validates *without* a
# depth key present at all, per hole_geometry.schema.yaml's conditional
# prohibition.

namespace: cuttingedgebespoke.feature
id: through_dowel_array
name: "Through dowel array — fixture only"
schema_version: "0.1"
object_version: "1.0.0"
status: draft
provenance:
  source_type: narrated
  source_reference: "hole_array typed Feature work package, 2026-07-30"
  captured_by: lucian
  captured_at: "2026-07-30T00:00:00Z"
  confidence: confirmed
  review_status: approved
  reviewed_by: lucian
  evidence: []
tags: [fixture, hole_array]

feature_type: hole_array
parameters:
  diameter: {literal: {value: 8, unit: mm}}
  hole_form: through
  pitch: {literal: {value: 32, unit: mm}}
  count: {literal: {value: 3, unit: count}}
  direction: {axis: y, sense: positive}

operation:
  ref: cuttingedgebespoke.operation.vertical_drill
  object_version: "1.0.0"
