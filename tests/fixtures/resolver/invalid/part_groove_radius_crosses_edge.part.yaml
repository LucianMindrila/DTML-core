# Invalid: anchor.x (2mm) is less than the groove's own radius
# (width 8mm / 2 = 4mm), so the swept capsule would cross the Part's
# left edge even though the bare centreline x (2mm) is >= 0. Proves
# the bounds check is radius-aware, not a bare 0 <= x <= width (mirrors
# part_hole_radius_crosses_edge.part.yaml for hole_array).

x-dtml-schema: ../../../../schemas/part.schema.yaml

namespace: cuttingedgebespoke.part
id: part_groove_radius_crosses_edge
name: "Groove radius crosses edge — resolver fixture"
schema_version: "0.1"
object_version: "1.0.0"
status: draft
provenance:
  source_type: narrated
  source_reference: "groove Feature work package, 2026-07-31"
  captured_by: lucian
  captured_at: "2026-07-31T00:00:00Z"
  confidence: confirmed
  review_status: approved
  reviewed_by: lucian
  evidence: []
tags: [fixture, resolver]
material:
  ref: cuttingedgebespoke.material.birch_plywood
  object_version: "1.0.0"

thickness:
  literal:
    value: 18
    unit: mm

dimensions:
  width:
    literal:
      value: 500
      unit: mm
  height:
    literal:
      value: 300
      unit: mm

reference_face: front

feature_instances:
  - feature:
      ref: cuttingedgebespoke.feature.straight_groove
      object_version: "1.0.0"
    position:
      x:
        literal:
          value: 2
          unit: mm
      y:
        literal:
          value: 150
          unit: mm
    reference_face: front
