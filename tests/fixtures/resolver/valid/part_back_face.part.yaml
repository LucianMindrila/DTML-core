# Valid: feature_instance.reference_face is back while the Part's own
# reference_face is front. Proves coordinate generation is identical
# regardless of which face is referenced — mirroring between front
# and back frames is a downstream concern this stage doesn't apply
# (SchemaConventions.md, "Face-local coordinates and depth").

x-dtml-schema: ../../../../schemas/part.schema.yaml

namespace: cuttingedgebespoke.part
id: part_back_face
name: "Back-face array — resolver fixture"
schema_version: "0.1"
object_version: "1.0.0"
status: draft
provenance:
  source_type: narrated
  source_reference: "Resolver v0.1 work package, 2026-07-30"
  captured_by: lucian
  captured_at: "2026-07-30T00:00:00Z"
  confidence: confirmed
  review_status: approved
  reviewed_by: lucian
  evidence: []
tags: [fixture, resolver]
material:
  ref: cuttingedgebespoke.material.egger_w1000_18mm
  object_version: "1.0.0"

construction: single_18mm

thickness:
  literal:
    value: 18
    unit: mm

dimensions:
  width:
    literal:
      value: 600
      unit: mm
  height:
    literal:
      value: 720
      unit: mm

reference_face: front

feature_instances:
  - feature:
      ref: cuttingedgebespoke.feature.shelf_pin_array
      object_version: "1.0.0"
    position:
      x:
        literal:
          value: 20
          unit: mm
      y:
        literal:
          value: 20
          unit: mm
    reference_face: back
