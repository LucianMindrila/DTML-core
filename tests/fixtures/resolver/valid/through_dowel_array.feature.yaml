x-dtml-schema: ../../../../schemas/features/hole_array.schema.yaml

# Resolver fixture: a through-hole hole_array, so the resolver's
# through-hole path (effective depth = Part thickness, no source depth to
# resolve) gets exercised end to end, not just at the schema layer (see
# tests/fixtures/valid/hole-array-through-hole.yaml for the schema-only
# version this mirrors).

namespace: cuttingedgebespoke.feature
id: through_dowel_array
name: "Through dowel array — resolver fixture"
schema_version: "0.1"
object_version: "1.0.0"
status: draft
provenance:
  source_type: narrated
  source_reference: "Resolver v0.1 work package, 2026-07-30"
  captured_by: lucian
  captured_at: "2026-07-30T00:00:00Z"
  confidence: confirmed
  review_status: approved
  reviewed_by: lucian
  evidence: []
tags: [fixture, resolver, hole_array]

feature_type: hole_array
parameters:
  diameter: {literal: {value: 8, unit: mm}}
  hole_form: through
  pitch: {literal: {value: 40, unit: mm}}
  count: {literal: {value: 3, unit: count}}
  start_offset: {literal: {value: 50, unit: mm}}
  direction: {axis: y, sense: positive}

operation:
  ref: cuttingedgebespoke.operation.vertical_drill
  object_version: "1.0.0"
