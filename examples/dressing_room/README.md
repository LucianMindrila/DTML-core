# examples/dressing_room

A worked example project for a full walk-in dressing room composition
(multiple Modules — hanging, drawers, shoe storage, shelving — combined
into one Furniture entry). See `../wardrobe/README.md` for the pattern
this follows, and `furniture.schema.yaml`'s open questions on multi-
Module composition before this example can be fully resolved.
