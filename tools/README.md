# tools

Supporting scripts and utilities that aren't part of the core engine or
the extractor (see `../extractor/` for DXF mining specifically). Expected
future contents: schema validation helpers, library linting (e.g.
flagging any `source_confidence: extracted_unconfirmed` entry that's
somehow been referenced by a production Module), and DXF diff/comparison
utilities supporting the verification workflow in `../tests/`.

Empty for now.
